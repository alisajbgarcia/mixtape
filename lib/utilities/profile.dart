import 'package:flutter/material.dart';

class Profile {
  String id;
  String spotifyUID;
  String displayName;
  String profilePicURL;

  Profile(this.id, this.spotifyUID, this.displayName, this.profilePicURL);
}